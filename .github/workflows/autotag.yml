# L'objectif de ce fichier aura pour but de créer des tags automatiquement lors de pull request
# v"major"."minor"."patch"
# Exemple de titre de la PR
# [major] - Ajout d'une nouvelle thématique de cours
# [minor] - Ajout d'un nouveau header ou phrases
# fix - Correction d'orthographe

name: 🚀 Auto Tag & Release

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  auto-version:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # on veut tous les tags

      - name: Configure git
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"

      - name: Get latest tag
        id: get_tag
        run: |
          latest_tag=$(git tag --sort=-v:refname | head -n 1)
          if [ -z "$latest_tag" ]; then
            latest_tag="v0.0.0"
          fi
          echo "latest_tag=$latest_tag" >> $GITHUB_ENV
          echo "Dernier tag détecté : $latest_tag"

      - name: Determine bump type
        id: bump
        run: |
          pr_title="${{ github.event.pull_request.title }}"
          pr_labels="${{ toJson(github.event.pull_request.labels) }}"
          bump="patch"

          if [[ "$pr_title" =~ \[major\] ]] || [[ "$pr_labels" == *"major"* ]]; then
            bump="major"
          elif [[ "$pr_title" =~ \[minor\] ]] || [[ "$pr_labels" == *"minor"* ]]; then
            bump="minor"
          fi

          echo "bump=$bump" >> $GITHUB_ENV
          echo "🧩 Type d’incrément : $bump"

      - name: Compute next version
        id: next
        run: |
          version=${{ env.latest_tag }}
          version=${version#v}  # supprime le 'v' au début
          IFS='.' read -r major minor patch <<< "$version"

          case "${{ env.bump }}" in
            major)
              major=$((major + 1))
              minor=0
              patch=0
              ;;
            minor)
              minor=$((minor + 1))
              patch=0
              ;;
            patch)
              patch=$((patch + 1))
              ;;
          esac

          next_tag="v$major.$minor.$patch"
          echo "next_tag=$next_tag" >> $GITHUB_ENV
          echo "✨ Nouveau tag : $next_tag"

      - name: Create and push tag
        run: |
          git tag ${{ env.next_tag }}
          git push origin ${{ env.next_tag }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.next_tag }}
          name: "🚀 Release ${{ env.next_tag }}"
          body: |
            ### Changement fusionné
            **PR #${{ github.event.pull_request.number }}** — ${{ github.event.pull_request.title }}

            Auteur : @${{ github.actor }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
